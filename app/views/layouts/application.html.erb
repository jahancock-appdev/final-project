<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
   <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li>[[New Outing as button?]]</li>
        <li><a href="/restaurants">Browse restaurants</a></li>
        
        <% if @current_user == nil%>
          <li>
            <a href="/user_sign_up">Sign up</a>
          </li>
          <li>
            <a href="/user_sign_in">Sign in</a>
          </li>
        <% else %>
          <li>
            <a href="/bookmarks">My saved restaurants</a></li>
          <li>
            <a href="/user_sign_out">Sign out</a>
          </li>
          <li>
            Outing details
          </li>
          <li>
            Restaurant details
          </li>
        <% end%>
      </ul>
    </nav>
    <p style="color: green"><%= notice %></p>
    <p style="color: red"><%= alert %></p>
    <%= yield %>
  </body>
</html>
